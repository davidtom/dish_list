<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title> <%= @user.name%>'s Dishlist </title>
  </head>
  <body>
    <center><h1> <%= @user.name%>'s Dishlist </h1></center>
    <form action="/users/<%= @user.id %>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <% @user.restaurants.each do |restaurant|%>
        <input type="checkbox" name= <%="user[restaurants][]#{restaurant.id}"%> <%=@user.visited?(restaurant)%>> <%=restaurant.name%> <br>
      <%end%>
      <input type="submit" value="Update Dishlist">
    </form>
  </body>
</html>
